Our aim is to make a GUI for Pygame

TODO:

Remove the margin and add it as an extra widget with one child
Make the rendering immediate mode
Simplify the events capture from the mouse
Make all the examples works
Make all the tests pass
Review the current code setup
Build a wheel and add as a pip package
